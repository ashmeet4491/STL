#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int>vec;
    vec.push_back(1);
    vec.push_back(2);
    vec.push_back(3);
    for(auto it=vec.rbegin();it!=vec.rend();++it)
    {
        cout<<(*it)<<"\n";
    }
bool res=vec.empty();
    cout<<res;
}
//if its empty it will return 1
//if its not empty it will return 0;
