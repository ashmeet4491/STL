#include <bits/stdc++.h>

using namespace std;

int main()
{
   
   vector<int>vec(5);
   vec.push_back(5);
   vec.push_back(6);
   vec.push_back(4);
   sort(vec.rbegin(),vec.rend());
   for(auto it=vec.begin();it!=vec.end();++it)
   {
       cout<<(*it)<<"\n";
   }
}
