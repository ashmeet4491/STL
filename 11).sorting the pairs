#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<pair<int,int>>a={{2,3},{3,4},{3,3}};
    sort(a.begin(),a.end());
        
        for(auto it=a.begin();it!=a.end();++it)
        {
            cout<<(*it).first<<(*it).second;
        }

    return 0;
}
