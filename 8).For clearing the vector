#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int>vec;
    vec.push_back(1);
    vec.push_back(2);
    vec.push_back(3);
    swap(vec[0],vec[1]);
    vec.clear();
    for(auto it=vec.begin();it!=vec.end();++it)
    {
        cout<<(*it)<<"\n";
    }

}
//vec.clear erases the memory;
