#include <bits/stdc++.h>

using namespace std;

int main()
{
    map<char,int>mp;
    string s;
    cin>>s;
    for(int i=0;s[i]!='\0';++i)
    {
        mp[s[i]]=mp[s[i]]+1;
    }
    for(auto it:{'a','e','i','o','u'})
    {
    cout<<it<<mp[it]<<"\n";
    }
}
