#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int>v1;
    v1.push_back(1);
    v1.push_back(2);
    v1.push_back(3);
    v1.erase(v1.begin()+1);
    for(auto it=v1.begin();it!=v1.end();++it)
    {
        cout<<(*it)<<endl;
    }
    
}
